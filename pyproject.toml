[project]
name = "du1-llm-tool-calling"
version = "0.1.0"
description = "Homework 1 - a Python script that calls an LLM API, uses tools and feeds the results back to the model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.0.0",
    "openai>=3.8.0",
    "python-dotenv>=1.0.1",
]
